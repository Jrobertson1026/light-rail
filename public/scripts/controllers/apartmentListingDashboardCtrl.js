var app = angular.module("lightRail")

app.controller("apartmentListingDashboardCtrl", function($scope) {
 /* var $scope.card = {};

 $scope.updateSubscription = function() {
   stripeService.updateSubscription($scope.user, $scope.card);
   $scope.card = {};
 }
*/

});
